<?xml version="1.0" encoding="utf-8"?>
<Window x:Class="BNKaraoke.DJ.Views.SongDetailsWindow"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        Width="500" Height="400" Background="#2D2D2D" WindowStyle="SingleBorderWindow" ResizeMode="NoResize"
        WindowStartupLocation="CenterScreen">
    <Grid Margin="20">
        <Grid.RowDefinitions>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="*"/>
            <RowDefinition Height="Auto"/>
        </Grid.RowDefinitions>
        <TextBlock Grid.Row="0" Text="{Binding SelectedQueueEntry.SongTitle}" 
                   FontSize="20" Foreground="#22d3ee" Margin="0,0,0,10" TextAlignment="Center"/>
        <StackPanel Grid.Row="1" Orientation="Vertical">
            <TextBlock Text="Artist:" Foreground="#ddd" FontSize="14" Margin="0,0,0,5">
                <Run Text="{Binding SelectedQueueEntry.SongArtist}"/>
            </TextBlock>
            <TextBlock Text="Requestor:" Foreground="#ddd" FontSize="14" Margin="0,0,0,5">
                <Run Text="{Binding SelectedQueueEntry.RequestorDisplayName}"/>
            </TextBlock>
            <TextBlock Text="Video Length:" Foreground="#ddd" FontSize="14" Margin="0,0,0,5">
                <Run Text="{Binding SelectedQueueEntry.VideoLength}"/>
            </TextBlock>
            <TextBlock Text="Genre:" Foreground="#ddd" FontSize="14" Margin="0,0,0,5">
                <Run Text="{Binding SelectedQueueEntry.Genre}"/>
            </TextBlock>
            <TextBlock Text="Decade:" Foreground="#ddd" FontSize="14" Margin="0,0,0,5">
                <Run Text="{Binding SelectedQueueEntry.Decade}"/>
            </TextBlock>
            <TextBlock Text="YouTube URL:" Foreground="#ddd" FontSize="14" Margin="0,0,0,5">
                <Run Text="{Binding SelectedQueueEntry.YouTubeUrl}"/>
            </TextBlock>
            <TextBlock Text="Cached:" Foreground="#ddd" FontSize="14" Margin="0,0,0,5">
                <Run Text="{Binding SelectedQueueEntry.IsVideoCached, StringFormat='{}{0}'}"/>
            </TextBlock>
        </StackPanel>
        <Button Grid.Row="2" Content="Close" Width="100" Height="40" HorizontalAlignment="Center" 
                Background="#22d3ee" Foreground="Black" Margin="0,10,0,0" Command="{Binding CloseCommand}"/>
    </Grid>
</Window>