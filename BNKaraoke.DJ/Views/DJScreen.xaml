<?xml version="1.0" encoding="utf-8"?>
<Window x:Class="BNKaraoke.DJ.Views.DJScreen"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        xmlns:local="clr-namespace:BNKaraoke.DJ.Converters"
        xmlns:i="http://schemas.microsoft.com/xaml/behaviors"
        xmlns:behaviors="clr-namespace:BNKaraoke.DJ.Behaviors"
        WindowState="Maximized" Title="DJ Console">
    <Window.Resources>
        <local:BooleanToVisibilityConverter x:Key="BooleanToVisibilityConverter" />
        <local:PlayPauseConverter x:Key="PlayPauseConverter" />
        <!-- New converter for Play/Pause icon -->
        <LinearGradientBrush x:Key="WindowBackground" StartPoint="0,0" EndPoint="0,1">
            <GradientStop Color="#1e3a8a" Offset="0"/>
            <GradientStop Color="#3b82f6" Offset="1"/>
        </LinearGradientBrush>
    </Window.Resources>
    <Window.Background>
        <StaticResource ResourceKey="WindowBackground"/>
    </Window.Background>
    <DockPanel>
        <!-- Header Area -->
        <Grid DockPanel.Dock="Top" Height="80" Background="#1E3A5F">
            <Grid.ColumnDefinitions>
                <ColumnDefinition Width="Auto"/>
                <ColumnDefinition Width="*"/>
                <ColumnDefinition Width="Auto"/>
            </Grid.ColumnDefinitions>
            <Image Source="pack://application:,,,/Assets/TwoSingerMnt.png" Width="70" Margin="10,0" Stretch="Uniform" Grid.Column="0"/>
            <TextBlock Text="BNKaraoke.com DJ Control Center" FontSize="28" FontWeight="Bold" Foreground="White" 
                       VerticalAlignment="Center" HorizontalAlignment="Center" Grid.Column="1"/>
            <DockPanel Grid.Column="2" Margin="0,0,0,0" HorizontalAlignment="Right">
                <Button x:Name="SettingsButton" Width="40" Height="40" Margin="5,0,0,0" Background="Blue" 
                        Command="{Binding OpenSettingsCommand}" DockPanel.Dock="Right">
                    <Image Source="pack://application:,,,/Assets/gear3.png" Width="24" Height="24"/>
                </Button>
                <Button x:Name="LoginLogoutButton" Width="100" Height="40" Margin="5,0,5,0" 
                        Content="{Binding LoginLogoutButtonText}" Background="{Binding LoginLogoutButtonColor}"
                        Foreground="White" FontWeight="Bold" Command="{Binding LoginLogoutCommand}" DockPanel.Dock="Right"/>
                <Button x:Name="JoinEventButton" Width="150" Height="40" Margin="5,0,5,0" Content="{Binding JoinEventButtonText}" 
                        Background="{Binding JoinEventButtonColor}" 
                        Visibility="{Binding IsJoinEventButtonVisible, Converter={StaticResource BooleanToVisibilityConverter}, UpdateSourceTrigger=PropertyChanged}"
                        Command="{Binding JoinLiveEventCommand}" DockPanel.Dock="Right"/>
            </DockPanel>
        </Grid>
        <!-- Main Content -->
        <DockPanel>
            <!-- Control Area (Bottom) -->
            <DockPanel DockPanel.Dock="Bottom" Height="80" Background="#1E3A5F">
                <StackPanel Orientation="Horizontal" Margin="10">
                    <Button Width="80" Height="40" Margin="5" Command="{Binding PlayCommand}" Background="#22d3ee" Foreground="Black" FontWeight="Bold">
                        <TextBlock Text="{Binding IsPlaying, Converter={StaticResource PlayPauseConverter}}" FontSize="24" VerticalAlignment="Center" HorizontalAlignment="Center"/>
                    </Button>
                    <Button Width="80" Height="40" Margin="5" Command="{Binding StopCommand}" Background="#22d3ee" Foreground="Black" FontWeight="Bold">
                        <TextBlock Text="■" FontSize="24" VerticalAlignment="Center" HorizontalAlignment="Center"/>
                    </Button>
                    <Button Width="80" Height="40" Margin="5" Command="{Binding SkipCommand}" Background="#22d3ee" Foreground="Black" FontWeight="Bold">
                        <TextBlock Text="⏭" FontSize="24" VerticalAlignment="Center" HorizontalAlignment="Center"/>
                    </Button>
                    <Button Content="Launch Video" Width="120" Height="40" Margin="5" Command="{Binding LaunchVideoCommand}" Background="#22d3ee" Foreground="Black" FontWeight="Bold"/>
                    <TextBlock Text="Now Playing: [Placeholder]" FontSize="16" Foreground="White" VerticalAlignment="Center" Margin="10,0"/>
                    <TextBlock Text="Up Next: [Placeholder]" FontSize="16" Foreground="White" VerticalAlignment="Center" Margin="10,0"/>
                    <TextBlock Text="Timer: [Placeholder]" FontSize="16" Foreground="White" VerticalAlignment="Center" Margin="10,0"/>
                    <TextBlock Text="Warning: [Placeholder]" FontSize="16" Foreground="White" VerticalAlignment="Center" Margin="10,0"/>
                </StackPanel>
            </DockPanel>
            <!-- DJQueue and Singers Detail -->
            <Grid>
                <Grid.ColumnDefinitions>
                    <ColumnDefinition Width="3*"/>
                    <ColumnDefinition Width="1*"/>
                </Grid.ColumnDefinitions>
                <!-- DJQueue Area (Left 3/4) -->
                <Border BorderBrush="White" BorderThickness="3" Margin="5" Grid.Column="0" Background="#2D2D2D">
                    <DockPanel>
                        <TextBlock DockPanel.Dock="Top" Text="{Binding CurrentEvent.Description}" 
                                   FontSize="18" Foreground="#22d3ee" Margin="5">
                            <TextBlock.Effect>
                                <DropShadowEffect Color="#22d3ee" ShadowDepth="0" BlurRadius="5"/>
                            </TextBlock.Effect>
                        </TextBlock>
                        <TextBlock DockPanel.Dock="Top" Text="Total Songs: {Binding QueueEntries.Count}" 
                                   FontSize="14" Foreground="#ddd" Margin="5"/>
                        <ListView x:Name="QueueListView" ItemsSource="{Binding QueueEntries}" Margin="5" SelectedItem="{Binding SelectedQueueEntry}">
                            <i:Interaction.Behaviors>
                                <behaviors:DragDropBehavior DragCommand="{Binding StartDragCommand}" DropCommand="{Binding DropCommand}"/>
                            </i:Interaction.Behaviors>
                            <ListView.View>
                                <GridView>
                                    <GridViewColumn Header="Pos" Width="50" DisplayMemberBinding="{Binding Position}"/>
                                    <GridViewColumn Header="Song Title" Width="200" DisplayMemberBinding="{Binding SongTitle}"/>
                                    <GridViewColumn Header="Artist" Width="150" DisplayMemberBinding="{Binding SongArtist}"/>
                                    <GridViewColumn Header="Requestor" Width="150" DisplayMemberBinding="{Binding RequestorDisplayName}"/>
                                    <GridViewColumn Header="Length" Width="80" DisplayMemberBinding="{Binding VideoLength}"/>
                                    <GridViewColumn Header="Genre" Width="100" DisplayMemberBinding="{Binding Genre}"/>
                                    <GridViewColumn Header="Decade" Width="80" DisplayMemberBinding="{Binding Decade}"/>
                                </GridView>
                            </ListView.View>
                            <ListView.ItemContainerStyle>
                                <Style TargetType="ListViewItem">
                                    <Setter Property="Background" Value="#f0f0f0"/>
                                    <Setter Property="Padding" Value="10"/>
                                    <Setter Property="BorderBrush" Value="#ccc"/>
                                    <Setter Property="BorderThickness" Value="0,0,0,1"/>
                                    <Style.Triggers>
                                        <Trigger Property="IsMouseOver" Value="True">
                                            <Setter Property="Background" Value="#e0e0e0"/>
                                        </Trigger>
                                    </Style.Triggers>
                                </Style>
                            </ListView.ItemContainerStyle>
                            <ListView.ItemTemplate>
                                <DataTemplate>
                                    <StackPanel Orientation="Horizontal">
                                        <TextBlock Text="☰" Margin="5,0" Cursor="SizeAll" Width="20"/>
                                        <TextBlock Text="{Binding Position}" Width="50"/>
                                        <TextBlock Text="{Binding SongTitle}" Width="200"/>
                                        <TextBlock Text="{Binding SongArtist}" Width="150"/>
                                        <TextBlock Text="{Binding RequestorDisplayName}" Width="150"/>
                                        <TextBlock Text="{Binding VideoLength}" Width="80"/>
                                        <TextBlock Text="{Binding Genre}" Width="100"/>
                                        <TextBlock Text="{Binding Decade}" Width="80"/>
                                    </StackPanel>
                                </DataTemplate>
                            </ListView.ItemTemplate>
                            <ListView.InputBindings>
                                <MouseBinding Gesture="LeftClick" Command="{Binding ShowSongDetailsCommand}"/>
                            </ListView.InputBindings>
                        </ListView>
                    </DockPanel>
                </Border>
                <!-- Singers Detail Area (Right 1/4) -->
                <Border BorderBrush="White" BorderThickness="3" Margin="5" Grid.Column="1">
                    <DockPanel>
                        <TextBlock DockPanel.Dock="Top" Text="Singers Detail Area (Placeholder)" 
                                   FontSize="16" Foreground="White" Margin="5"/>
                        <ScrollViewer>
                            <TextBlock Text="Scrollable singer list, SignalR sync (/hubs/singers), click for singer details (Placeholder)" 
                                       FontSize="14" Foreground="White" Margin="5"/>
                        </ScrollViewer>
                    </DockPanel>
                </Border>
            </Grid>
        </DockPanel>
    </DockPanel>
</Window>