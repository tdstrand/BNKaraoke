<?xml version="1.0" encoding="utf-8"?>
<Window x:Class="BNKaraoke.DJ.Views.DJScreen"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        xmlns:local="clr-namespace:BNKaraoke.DJ.Converters"
        xmlns:i="http://schemas.microsoft.com/xaml/behaviors"
        xmlns:behaviors="clr-namespace:BNKaraoke.DJ.Behaviors"
        WindowState="Maximized" Title="DJ Console"
        Loaded="Window_Loaded">
    <Window.Resources>
        <local:BooleanToVisibilityConverter x:Key="BooleanToVisibility" />
        <local:PlayPauseConverter x:Key="PlayPauseIcon" />
        <local:SingerStatusToColorConverter x:Key="SingerStatusToColorConverter" />
        <local:VideoLengthConverter x:Key="VideoLengthConverter" />
        <local:UpNextConverter x:Key="UpNextConverter" />
        <local:TimeRemainingConverter x:Key="TimeRemainingConverter" />
        <LinearGradientBrush x:Key="WindowBackground" StartPoint="0,0" EndPoint="0,1">
            <GradientStop Color="#1e3a8a" Offset="0"/>
            <GradientStop Color="#3b82f6" Offset="1"/>
        </LinearGradientBrush>
    </Window.Resources>
    <Window.Background>
        <StaticResource ResourceKey="WindowBackground"/>
    </Window.Background>
    <DockPanel>
        <!-- Header Area -->
        <Grid DockPanel.Dock="Top" Height="80" Background="#1E3A5F">
            <Grid.ColumnDefinitions>
                <ColumnDefinition Width="Auto"/>
                <ColumnDefinition Width="*"/>
                <ColumnDefinition Width="Auto"/>
            </Grid.ColumnDefinitions>
            <Image Source="pack://application:,,,/Assets/TwoSingerMnt.png" Width="70" Margin="10,0" Stretch="Uniform" Grid.Column="0"/>
            <TextBlock Text="BNKaraoke.com DJ Control Center" FontSize="28" FontWeight="Bold" Foreground="White"
                       VerticalAlignment="Center" HorizontalAlignment="Center" Grid.Column="1"/>
            <DockPanel Grid.Column="2" Margin="0,0,0,0" HorizontalAlignment="Right">
                <Button x:Name="SettingsButton" Width="40" Height="40" Margin="5,0,0,0" Background="Blue"
                        Command="{Binding OpenSettingsCommand}" DockPanel.Dock="Right">
                    <Image Source="pack://application:,,,/Assets/gear3.png" Width="24" Height="24"/>
                </Button>
                <Button x:Name="LoginLogoutButton" Width="100" Height="40" Margin="5,0,5,0"
                        Content="{Binding LoginLogoutButtonText, UpdateSourceTrigger=PropertyChanged}"
                        Background="{Binding LoginLogoutButtonColor, UpdateSourceTrigger=PropertyChanged}"
                        Foreground="White" FontWeight="Bold" Command="{Binding LoginLogoutCommand}" DockPanel.Dock="Right"/>
                <Button x:Name="JoinEventButton" Width="150" Height="40" Margin="5,0,5,0"
                        Content="{Binding JoinEventButtonText, UpdateSourceTrigger=PropertyChanged}"
                        Background="{Binding JoinEventButtonColor, UpdateSourceTrigger=PropertyChanged}"
                        Visibility="{Binding IsJoinEventButtonVisible, Converter={StaticResource BooleanToVisibility}, UpdateSourceTrigger=PropertyChanged}"
                        Command="{Binding JoinLiveEventCommand}"/>
            </DockPanel>
        </Grid>
        <!-- Main Content -->
        <DockPanel>
            <!-- Control Area (Bottom) -->
            <DockPanel DockPanel.Dock="Bottom" Height="80" Background="#1E3A5F">
                <Border Width="120" Height="40" BorderBrush="White" BorderThickness="1" Margin="10,0" Padding="5" DockPanel.Dock="Right">
                    <Border.Style>
                        <Style TargetType="Border">
                            <Setter Property="Background" Value="Green"/>
                            <Style.Triggers>
                                <DataTrigger Binding="{Binding TimeRemainingSeconds, Converter={StaticResource TimeRemainingConverter}, ConverterParameter=30}" Value="True">
                                    <Setter Property="Background" Value="#FFA500"/>
                                </DataTrigger>
                                <DataTrigger Binding="{Binding TimeRemainingSeconds, Converter={StaticResource TimeRemainingConverter}, ConverterParameter=15}" Value="True">
                                    <Setter Property="Background" Value="Red"/>
                                </DataTrigger>
                                <DataTrigger Binding="{Binding IsPlaying}" Value="False">
                                    <Setter Property="Visibility" Value="Collapsed"/>
                                </DataTrigger>
                            </Style.Triggers>
                        </Style>
                    </Border.Style>
                    <TextBlock Text="{Binding TimeRemaining}" FontSize="18" FontWeight="Bold" Foreground="White" VerticalAlignment="Center" HorizontalAlignment="Center"/>
                </Border>
                <StackPanel Orientation="Horizontal" Margin="10">
                    <Button Width="80" Height="40" Margin="5" Command="{Binding PlayCommand}" Background="#22d3ee" Foreground="Black" FontWeight="Bold">
                        <TextBlock Text="{Binding IsPlaying, Converter={StaticResource PlayPauseIcon}}" FontSize="24" VerticalAlignment="Center" HorizontalAlignment="Center"/>
                    </Button>
                    <Button Width="80" Height="40" Margin="5" Command="{Binding StopCommand}" Background="#22d3ee" Foreground="Black" FontWeight="Bold">
                        <TextBlock Text="⏹" FontSize="24" VerticalAlignment="Center" HorizontalAlignment="Center"/>
                    </Button>
                    <Button Width="80" Height="40" Margin="5" Command="{Binding SkipCommand}" Background="#22d3ee" Foreground="Black" FontWeight="Bold">
                        <TextBlock Text="⏭" FontSize="24" VerticalAlignment="Center" HorizontalAlignment="Center"/>
                    </Button>
                    <Button Width="120" Height="40" Margin="5" Command="{Binding ToggleShowCommand}" 
                            Content="{Binding ShowButtonText, UpdateSourceTrigger=PropertyChanged}"
                            Background="{Binding ShowButtonColor, UpdateSourceTrigger=PropertyChanged}" 
                            Foreground="Black" FontWeight="Bold"/>
                    <Button Width="120" Height="40" Margin="5" Command="{Binding ToggleAutoPlayCommand}" 
                            Content="{Binding AutoPlayButtonText, UpdateSourceTrigger=PropertyChanged}"
                            Background="#22d3ee" Foreground="Black" FontWeight="Bold"/>
                    <TextBlock FontSize="16" Foreground="White" VerticalAlignment="Center" Margin="10,0">
                        <TextBlock.Text>
                            <MultiBinding Converter="{StaticResource VideoLengthConverter}" FallbackValue="Timer: --:-- / --:--">
                                <Binding Path="CurrentVideoPosition" />
                                <Binding Path="PlayingQueueEntry.VideoLength" />
                            </MultiBinding>
                        </TextBlock.Text>
                    </TextBlock>
                    <TextBlock Text="{Binding QueueEntries[0], StringFormat=Up Next: {0} by {1}, FallbackValue='Up Next: None', Converter={StaticResource UpNextConverter}}" 
                               FontSize="16" Foreground="White" VerticalAlignment="Center" Margin="10,0"/>
                </StackPanel>
            </DockPanel>
            <!-- DJQueue and Singers Detail -->
            <Grid>
                <Grid.ColumnDefinitions>
                    <ColumnDefinition Width="3*"/>
                    <ColumnDefinition Width="1*"/>
                </Grid.ColumnDefinitions>
                <!-- DJQueue Area (Left 3/4) -->
                <Border BorderBrush="White" BorderThickness="3" Margin="5" Grid.Column="0" Background="#2D2D2D">
                    <DockPanel>
                        <TextBlock DockPanel.Dock="Top" Text="{Binding CurrentEvent.Description, FallbackValue='No Event'}"
                                   FontSize="18" Foreground="#22d3ee" Margin="5">
                            <TextBlock.Effect>
                                <DropShadowEffect Color="#22d3ee" ShadowDepth="0" BlurRadius="5"/>
                            </TextBlock.Effect>
                        </TextBlock>
                        <DockPanel DockPanel.Dock="Top" Margin="5">
                            <TextBlock Text="{Binding QueueEntries.Count, StringFormat='Queued Songs: {0}'}" FontSize="14" Foreground="#ddd" DockPanel.Dock="Left"/>
                            <TextBlock Text="{Binding TotalSongsPlayed, StringFormat='Total Songs Played: {0}'}" FontSize="14" Foreground="#ddd" DockPanel.Dock="Right" HorizontalAlignment="Right"/>
                        </DockPanel>
                        <!-- Now Playing Area -->
                        <Border DockPanel.Dock="Top" Margin="5">
                            <Border.Style>
                                <Style TargetType="Border">
                                    <Style.Triggers>
                                        <DataTrigger Binding="{Binding IsPlaying}" Value="True">
                                            <Setter Property="BorderBrush" Value="#FF0000"/>
                                            <Setter Property="BorderThickness" Value="2"/>
                                        </DataTrigger>
                                    </Style.Triggers>
                                </Style>
                            </Border.Style>
                            <Grid Visibility="{Binding IsPlaying, Converter={StaticResource BooleanToVisibilityConverter}}">
                                <Grid.RowDefinitions>
                                    <RowDefinition Height="Auto"/>
                                    <RowDefinition Height="Auto"/>
                                </Grid.RowDefinitions>
                                <Grid.ColumnDefinitions>
                                    <ColumnDefinition Width="50"/>
                                    <ColumnDefinition Width="250"/>
                                    <ColumnDefinition Width="200"/>
                                    <ColumnDefinition Width="150"/>
                                    <ColumnDefinition Width="80"/>
                                    <ColumnDefinition Width="50"/>
                                </Grid.ColumnDefinitions>
                                <TextBlock Text="Now Playing" Grid.Row="0" Grid.ColumnSpan="6" FontSize="18" FontWeight="Bold" Foreground="#FF0000" Margin="5"/>
                                <TextBlock Text="{Binding PlayingQueueEntry.Position}" Grid.Row="1" Grid.Column="0" FontSize="18" Foreground="#ddd" Margin="5"/>
                                <TextBlock Text="{Binding PlayingQueueEntry.SongTitle, FallbackValue=''}" Grid.Row="1" Grid.Column="1" FontSize="18" Foreground="#ddd" Margin="5"/>
                                <TextBlock Text="{Binding PlayingQueueEntry.SongArtist, FallbackValue=''}" Grid.Row="1" Grid.Column="2" FontSize="18" Foreground="#ddd" Margin="5"/>
                                <TextBlock Text="{Binding PlayingQueueEntry.RequestorDisplayName, FallbackValue=''}" Grid.Row="1" Grid.Column="3" FontSize="18" Foreground="#ddd" Margin="5"/>
                                <TextBlock Text="{Binding PlayingQueueEntry.VideoLength}" Grid.Row="1" Grid.Column="4" FontSize="18" Foreground="#ddd" Margin="5"/>
                                <CheckBox IsChecked="{Binding PlayingQueueEntry.IsVideoCached}" IsEnabled="False" Grid.Row="1" Grid.Column="5" HorizontalAlignment="Center" VerticalAlignment="Center"/>
                            </Grid>
                        </Border>
                        <ListView x:Name="QueueListView" ItemsSource="{Binding QueueEntries}" Margin="5"
                                  SelectedItem="{Binding SelectedQueueEntry}" AllowDrop="True">
                            <i:Interaction.Behaviors>
                                <behaviors:DragDropBehavior DropCommand="{Binding DropCommand}"/>
                            </i:Interaction.Behaviors>
                            <ListView.View>
                                <GridView>
                                    <GridViewColumn Header="Pos" Width="50" DisplayMemberBinding="{Binding Position}"/>
                                    <GridViewColumn Header="Song Title" Width="250" DisplayMemberBinding="{Binding SongTitle}"/>
                                    <GridViewColumn Header="Artist" Width="200" DisplayMemberBinding="{Binding SongArtist}"/>
                                    <GridViewColumn Header="Requestor" Width="150" DisplayMemberBinding="{Binding RequestorDisplayName}"/>
                                    <GridViewColumn Header="Length" Width="80" DisplayMemberBinding="{Binding VideoLength}"/>
                                    <GridViewColumn Header="Cached" Width="50">
                                        <GridViewColumn.CellTemplate>
                                            <DataTemplate>
                                                <CheckBox IsChecked="{Binding IsVideoCached}" IsEnabled="False" HorizontalAlignment="Center"/>
                                            </DataTemplate>
                                        </GridViewColumn.CellTemplate>
                                    </GridViewColumn>
                                </GridView>
                            </ListView.View>
                            <ListView.ItemContainerStyle>
                                <Style TargetType="ListViewItem">
                                    <Setter Property="FontSize" Value="18"/>
                                    <Setter Property="Background" Value="#f0f0f0"/>
                                    <Setter Property="Padding" Value="10"/>
                                    <Setter Property="BorderBrush" Value="#ccc"/>
                                    <Setter Property="BorderThickness" Value="0,0,0,1"/>
                                    <Setter Property="Template">
                                        <Setter.Value>
                                            <ControlTemplate TargetType="ListViewItem">
                                                <Border x:Name="Border" Background="{TemplateBinding Background}"
                                                        BorderBrush="{TemplateBinding BorderBrush}"
                                                        BorderThickness="{TemplateBinding BorderThickness}">
                                                    <Grid>
                                                        <Rectangle x:Name="DropIndicator" Height="2" Fill="#22D3EE" Visibility="Collapsed"
                                                                   VerticalAlignment="Top"/>
                                                        <ContentPresenter/>
                                                    </Grid>
                                                </Border>
                                                <ControlTemplate.Triggers>
                                                    <Trigger Property="IsMouseOver" Value="True">
                                                        <Setter TargetName="DropIndicator" Property="Visibility" Value="Visible"/>
                                                        <Setter Property="Background" Value="#e0e0e0"/>
                                                    </Trigger>
                                                    <Trigger Property="IsSelected" Value="True">
                                                        <Setter Property="Background" Value="#d1d5db"/>
                                                    </Trigger>
                                                </ControlTemplate.Triggers>
                                            </ControlTemplate>
                                        </Setter.Value>
                                    </Setter>
                                    <EventSetter Event="PreviewMouseLeftButtonDown" Handler="ListViewItem_PreviewMouseLeftButtonDown"/>
                                    <EventSetter Event="MouseDoubleClick" Handler="ListViewItem_MouseDoubleClick"/>
                                    <Setter Property="ContextMenu">
                                        <Setter.Value>
                                            <ContextMenu>
                                                <MenuItem Header="Show Details" Command="{Binding DataContext.ShowSongDetailsCommand, RelativeSource={RelativeSource AncestorType=ListView}}"/>
                                            </ContextMenu>
                                        </Setter.Value>
                                    </Setter>
                                </Style>
                            </ListView.ItemContainerStyle>
                            <ListView.ItemTemplate>
                                <DataTemplate>
                                    <StackPanel Orientation="Horizontal">
                                        <TextBlock Text="{Binding Position}" FontSize="18" Width="50"/>
                                        <TextBlock Text="{Binding SongTitle}" FontSize="18" Width="250"/>
                                        <TextBlock Text="{Binding SongArtist}" FontSize="18" Width="200"/>
                                        <TextBlock Text="{Binding RequestorDisplayName}" FontSize="18" Width="150"/>
                                        <TextBlock Text="{Binding VideoLength}" FontSize="18" Width="80"/>
                                        <CheckBox IsChecked="{Binding IsVideoCached}" IsEnabled="False" Width="50"/>
                                    </StackPanel>
                                </DataTemplate>
                            </ListView.ItemTemplate>
                        </ListView>
                    </DockPanel>
                </Border>
                <!-- Singers Detail Area (Right 1/4) -->
                <Border BorderBrush="White" BorderThickness="3" Margin="5" Grid.Column="1" Background="#2D2D2D">
                    <DockPanel>
                        <TextBlock DockPanel.Dock="Top" Text="Singers" FontSize="16" Foreground="#22d3ee" Margin="5">
                            <TextBlock.Effect>
                                <DropShadowEffect Color="#22d3ee" ShadowDepth="0" BlurRadius="5"/>
                            </TextBlock.Effect>
                        </TextBlock>
                        <TextBlock DockPanel.Dock="Top" Text="{Binding NonDummySingersCount, StringFormat='Total Singers: {0}'}" FontSize="14" Foreground="#ddd" Margin="5"/>
                        <ScrollViewer VerticalScrollBarVisibility="Auto">
                            <StackPanel Margin="5">
                                <!-- Green Singers -->
                                <ItemsControl ItemsSource="{Binding GreenSingers}">
                                    <ItemsControl.ItemsPanel>
                                        <ItemsPanelTemplate>
                                            <WrapPanel Width="260" />
                                        </ItemsPanelTemplate>
                                    </ItemsControl.ItemsPanel>
                                    <ItemsControl.ItemTemplate>
                                        <DataTemplate>
                                            <Grid Width="130">
                                                <TextBlock Text="{Binding DisplayName, FallbackValue='Unknown Singer'}"
                                                           FontSize="16" Margin="5"
                                                           Foreground="{Binding ., Converter={StaticResource SingerStatusToColorConverter}}"/>
                                            </Grid>
                                        </DataTemplate>
                                    </ItemsControl.ItemTemplate>
                                </ItemsControl>
                                <!-- Yellow Singers -->
                                <ItemsControl ItemsSource="{Binding YellowSingers}">
                                    <ItemsControl.ItemsPanel>
                                        <ItemsPanelTemplate>
                                            <WrapPanel Width="260" />
                                        </ItemsPanelTemplate>
                                    </ItemsControl.ItemsPanel>
                                    <ItemsControl.ItemTemplate>
                                        <DataTemplate>
                                            <Grid Width="130">
                                                <TextBlock Text="{Binding DisplayName, FallbackValue='Unknown Singer'}"
                                                           FontSize="16" Margin="5"
                                                           Foreground="{Binding ., Converter={StaticResource SingerStatusToColorConverter}}"/>
                                            </Grid>
                                        </DataTemplate>
                                    </ItemsControl.ItemTemplate>
                                </ItemsControl>
                                <!-- Orange Singers -->
                                <ItemsControl ItemsSource="{Binding OrangeSingers}">
                                    <ItemsControl.ItemsPanel>
                                        <ItemsPanelTemplate>
                                            <WrapPanel Width="260" />
                                        </ItemsPanelTemplate>
                                    </ItemsControl.ItemsPanel>
                                    <ItemsControl.ItemTemplate>
                                        <DataTemplate>
                                            <Grid Width="130">
                                                <TextBlock Text="{Binding DisplayName, FallbackValue='Unknown Singer'}"
                                                           FontSize="16" Margin="5"
                                                           Foreground="{Binding ., Converter={StaticResource SingerStatusToColorConverter}}"/>
                                            </Grid>
                                        </DataTemplate>
                                    </ItemsControl.ItemTemplate>
                                </ItemsControl>
                                <!-- Red Singers -->
                                <ItemsControl ItemsSource="{Binding RedSingers}">
                                    <ItemsControl.ItemsPanel>
                                        <ItemsPanelTemplate>
                                            <WrapPanel Width="260" />
                                        </ItemsPanelTemplate>
                                    </ItemsControl.ItemsPanel>
                                    <ItemsControl.ItemTemplate>
                                        <DataTemplate>
                                            <Grid Width="130">
                                                <TextBlock Text="{Binding DisplayName, FallbackValue='Unknown Singer'}"
                                                           FontSize="16" Margin="5"
                                                           Foreground="{Binding ., Converter={StaticResource SingerStatusToColorConverter}}"/>
                                            </Grid>
                                        </DataTemplate>
                                    </ItemsControl.ItemTemplate>
                                </ItemsControl>
                            </StackPanel>
                        </ScrollViewer>
                    </DockPanel>
                </Border>
            </Grid>
        </DockPanel>
    </DockPanel>
</Window>